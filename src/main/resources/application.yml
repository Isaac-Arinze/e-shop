spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

  application:
    name: e-shop
  datasource:
    url: jdbc:mysql://localhost:3306/e_shop
    username: root
    password: zikans@19
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    showSql:
      true
    properties:
      hibernate:
        format-sql:
          true
server:
  port:
    9191

api:
  prefix:
    api/v1